# TraceBox Agent — Python bağımlılıkları
#
# Sürümler == ile SABİTLENDİ (collector/requirements.txt ile aynı gerekçe):
# install.sh bu dosyayı /opt/tracebox/venv içine kurar ve her cihaza birebir
# aynı sürümün gitmesi gerekir. Yükseltme bilinçli bir işlem olmalı.

# Metrik ve envanter kaynağı: CPU, RAM, disk, ağ sayaçları, çekirdek sayısı,
# açılış zamanı. Platform farklarını kendi içinde soğurur — /proc'u elle
# ayrıştırmak yerine tek bir arayüz verir (istisna: işlemci MODELİ psutil'de
# yok, o /proc/cpuinfo'dan okunuyor).
psutil==7.1.0

# Collector'a POST için HTTP istemcisi (shipper.py). Zaman aşımı, bağlantı
# havuzu ve TLS doğrulaması tek arayüzde; collector tarafında da aynı kütüphane
# kullanılıyor.
httpx==0.28.1

# M4 journald okuması için ek bağımlılık YOK: loglar `journalctl --output=json` çıktısından
# okunur, ayrıştırma standart kütüphanedeki json ile yapılır. journald zaten kurulumun ön
# koşulu olduğu için journalctl her hedef makinede vardır.
#
# spool SQLite ile tutulur: sqlite3 standart kütüphanede, ek bağımlılık yok.
